#!/bin/bash
# Check: Static type checking with mypy
set -e

echo "Running type checking..."
uv run mypy projector/ --ignore-missing-imports 2>/dev/null || {
    echo "Note: Type checking not fully configured"
    exit 0
}
echo "✓ Type checking completed"
